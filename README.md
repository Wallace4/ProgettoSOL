#Come eseguire il file
Nella cartella del progetto, quindi *MICHELE DE QUATTRO/* far partire il comando *./bin/server* per far partire il server,
ed il comando `make test` per far partire la batteria di test. `make test` NON fa partire il server.
